const config = {
    app: {
        port: process.env.port || 8086,
        base: '/api',
    },
    database: {
        host: '',
        dbname: '',
        username: '',
        password: '',

    }
}

export default config;